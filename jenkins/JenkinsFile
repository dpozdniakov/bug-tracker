pipeline {
    agent any

    stages {
        stage('Utit tests - Backend') {
            steps {
                dir('bugracker-backend') {
                    go test -v ./...
                }
            }
        }
    }
}
